name: know_your_system
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  battery_plus: ^7.0.0
  connectivity_plus: ^7.0.0
  device_info_plus: ^12.3.0
  fl_chart: ^1.1.1
  google_fonts: ^8.0.1
  intl: ^0.20.2
  path_provider: ^2.1.5
  provider: ^6.1.5+1
  win32: ^5.15.0
  window_manager: ^0.5.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
